# Video Frame Sampler

Sample frames from a local video and assemble them into a short MP4 preview. The source video stays on the local machine.

Requires FFmpeg and ffprobe on the system path, `/opt/homebrew/bin`, or `/usr/local/bin`.
