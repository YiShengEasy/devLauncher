import type { BuiltinManifest } from "../types";

export const manifest: BuiltinManifest = {
  id: "projecttasks",
  name: "项目任务",
  description: "发现 Markdown 中的 Runme 任务并发送到终端或保存为工作流",
  emoji: "RUN",
  window: { width: 1180, height: 720, resizable: true, alwaysOnTop: false },
};
