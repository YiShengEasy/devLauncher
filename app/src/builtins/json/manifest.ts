import type { BuiltinManifest } from "../types";

export const manifest: BuiltinManifest = {
  id: "json",
  name: "JSON 助手",
  description: "格式化、转义/去转义、生成 OpenAI 文档",
  emoji: "{ }",
  window: { width: 700, height: 620, resizable: true, alwaysOnTop: true },
};
